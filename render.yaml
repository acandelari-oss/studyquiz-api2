services:
  - type: web
    name: studyquiz-api2-ocr
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    aptPackages:
      - tesseract-ocr
      - poppler-utils